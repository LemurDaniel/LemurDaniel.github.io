
services:
  terraform-playground:
    build:
      context: ../../TERRAFORM__Coding-Playground
      dockerfile: Dockerfile
    container_name: terraform-playground
    restart: unless-stopped
    environment:
      - VIRTUAL_HOST=playground.lemurdaniel.de
      - VIRTUAL_PORT=80
      - LETSENCRYPT_HOST=playground.lemurdaniel.de
      - LETSENCRYPT_EMAIL=landau.daniel.1998@gmail.com
    networks:
      - backend

networks:
  backend:
    external: true
    name: backend
